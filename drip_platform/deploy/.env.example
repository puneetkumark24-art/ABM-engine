# Copy to deploy/.env and fill in. docker compose reads it automatically.

# --- database passwords ---
POSTGRES_PASSWORD=change-postgres-superuser-pw
APP_DB_PASSWORD=change-app_rw-runtime-pw

# --- security ---
# 32+ random bytes. In production use a rotated key from your secrets manager.
JWT_SECRET=change-me-to-a-long-random-secret
# webhook HMAC key from your ESP (Mandrill). Leave blank until wired.
MANDRILL_WEBHOOK_KEY=

# --- sending (kept OFF until you enable it deliberately) ---
ENABLE_SES_TRANSPORT=false
AWS_SES_REGION=me-south-1
SES_FROM=noreply@decimaltechnologies.com
SES_REPLY_TO=
SES_CONFIGURATION_SET=drip-transactional
# SNS delivery topic subscribed to an encrypted SQS queue with a DLQ.
SES_EVENT_TOPIC_ARN=
SES_EVENT_QUEUE_URL=
SES_SQS_VISIBILITY_SECONDS=120
# Keep false while DRIP's own open pixel and click redirect are enabled, or
# engagement would be counted from two tracking systems.
SES_USE_PROVIDER_ENGAGEMENT=false

# Mailchimp Transactional (Mandrill) — alternative native send transport,
# ported from decimal_abm (AI Intelligence Layer Sprint 4). Off by default.
ENABLE_MANDRILL_TRANSPORT=false
MANDRILL_API_KEY=
MANDRILL_FROM_EMAIL=noreply@decimaltechnologies.com
MANDRILL_FROM_NAME=Decimal Technologies

# --- behavior ---
# AUTH_ENFORCED=true rejects unauthenticated calls on protected routes.
AUTH_ENFORCED=true
SCHEDULER_TICK=15
RESPECT_SEND_WINDOW=true
SCHEDULER_TIMEZONE=Asia/Riyadh
WEB_CONCURRENCY=2
WORKER_BATCH=20
